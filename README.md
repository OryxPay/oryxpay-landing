**OryxPay Landing**

Projeto React + Vite para a landing page da OryxPay.

**Executar localmente**

1. Instale as dependencias: `npm install`
2. Inicie o servidor de desenvolvimento: `npm run dev`

Nao ha configuracao obrigatoria de `.env` para subir o projeto.
